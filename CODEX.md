# 코드 변환기 — Codex / Claude Code 인계

마지막 갱신: 2026-09-04 17:17 KST

이 문서는 웹 찬송집의 악보 데이터 미반영 진단과 후속 자동화 작업을 Claude Code가 바로 이어서 수행할 수 있도록 정리한 인계 문서다. 전체 설계 결정은 `../코드변환기 계획.md`, 웹 사용법은 `README.md`를 먼저 함께 읽는다.

## 세션 재개용 현재 상태 — 먼저 읽기

작업 위치는 `web/`이며 공개 저장소와 Pages는 이미 연결되어 있다. 기능 기준 최신 커밋은 `24e0e79` (`Add favorites-only song filter`)이다. 직전 배포 `5841687`의 Pages 빌드와 공개 `index.html` blob 일치는 확인 완료했으며, 이번 필터 커밋도 아래 문서 커밋과 함께 `origin/main`에 push한다.

### 방금 완료한 사용자 요청

- 목록 상단을 `검색 | 즐겨찾기 필터 별 | 테마`의 3열로 바꿨다. 필터 별을 누르면 저장된 즐겨찾기만 같은 곡 번호·제목·조·박자 행으로 보이고, 다시 누르면 전체 목록으로 돌아온다.
- 검색어와 즐겨찾기 필터는 AND로 결합한다. 필터 상태는 현재 SPA 세션에서 상세 진입·목록 복귀 동안 유지하지만, 새 방문이 빈 필터 화면으로 시작하지 않도록 localStorage에는 저장하지 않는다.
- 필터 중 목록/상세에서 별을 해제하거나 다른 탭에서 storage set/remove/clear가 발생하면 결과를 즉시 다시 계산한다. 키보드 포커스는 같은 행, 다음 행, 이전 행, 필터 버튼 순으로 복구한다.
- 상세를 보는 동안 외부 탭이 목록 구성을 크게 바꿔도 복귀할 곡을 `focusAndReveal`로 화면 중앙에 드러내 포커스 링이 잘리지 않게 한다.
- 자동 스크롤 속도 범위를 1~15로 확장했다. 390px에서 001·026·112를 속도 15로 각각 2초 실측한 결과는 22.42·22.63·22.39px/s다.
- 정수 `scrollY` 사이의 소수 거리를 `#song`의 1px 미만 `translate3d`로 보간한다. 1.216초/74프레임 측정에서 이동 가능한 73프레임 전부 움직였고 멈춘 프레임은 0이었다.
- 목록 행과 곡 제목에 44px 별 버튼을 추가했다. 즐겨찾기 ID 배열은 `localStorage`의 `song-of-songs.favorites.v1`에 저장한다.
- 즐겨찾기는 검색·목록·상세·새로고침·다른 탭 `setItem`·다른 탭 `clear()` 사이에서 동기화된다. 저장 실패 시 표시 상태를 되돌리고, 토글 직전 저장값을 다시 읽어 탭 간 덮어쓰기 가능성을 줄였다.
- 키보드로 곡을 열면 제목으로 포커스를 옮기고, 목록 또는 브라우저 Back으로 돌아오면 열었던 곡 버튼과 목록 스크롤 위치를 함께 복원한다.
- 백그라운드 복귀 시 Wake Lock을 다시 요청하고 누적 시간 점프를 막는다. 방향키·PageUp/Down·Home/End 수동 스크롤은 자동 스크롤을 멈추며, 사라지는 속도 버튼의 포커스는 재생 버튼으로 옮긴다.
- 상단 테마 아이콘, 목록 별, 제목 별, 하단 7칸의 아이콘/라벨 중심을 재검토했다. 320·390·834·1440px의 라이트/다크에서 아이콘 중심 오차와 가로 넘침은 모두 0px다.
- 4방향 `safe-area-inset-*`, 버튼 hover/active 대비, 재생 중 active cascade, SVG 재생/정지 `hidden` 토글을 보정했다.
- 조 선택 UI는 큰 세로 목록이 아니라 작은 4×3 글래스 그리드이며 359px 이하는 3열이다. 이후 작업에서 큰 패널로 되돌리지 않는다.

### 구현 위치

- 즐겨찾기 저장·복구·동기화: `src/app.js` 상단과 `toggleFavorite`, `storage` 이벤트.
- 즐겨찾기 필터·검색 교집합·필터 목록 포커스 복원: `src/app.js`의 `setFavoriteFilterButton`~`refreshFavoriteResults`, `renderList`, `route`.
- 목록/상세 포커스 및 스크롤 복원: `src/app.js`의 `renderList`, `route`, `openSong`.
- 자동 스크롤 보간·속도·Wake Lock: `src/app.js`의 `SCROLL_PX_PER_SPEED` 아래 `drawScrollPosition`~`showSpeed`.
- 별 SVG와 상세 제목 버튼: `src/song of songs.html`.
- 안전영역·별·아이콘 중앙 정렬·상태 대비: `src/app.css`.
- 사용자 문서: `README.md`. 생성물 `index.html`은 직접 편집하지 않는다.

### 마지막 검증

- `node --check web/src/app.js`: 통과.
- `node web/tools/test_engine.js`: 184곡 · 코드 116종 · 쌓기 가능 96곡 · 실패 0.
- `python web/tools/build.py`: `index.html` 약 590KB · 외부 참조 0.
- 브라우저 런타임 예외 0, 하단 표시 버튼 7개, 최소 터치 영역 44px, 390px 툴바 넘침 0.
- 필터 실측: favorites 001·026·207 → 3행, 검색 026 → 1행, 검색 112 → 0행. 직접 `#112` 진입과 하단 7개 기능은 필터 중에도 정상이다.
- 실제 동일 origin 두 탭에서 set 후 001·026 2행, `localStorage.clear()` 후 0행으로 즉시 동기화됐다. 키보드로 026 별 해제 후 다음 207 별에 보이는 포커스가 유지됐다.
- 320·390·834·1440px 라이트/다크에서 상단 가로 넘침 0, 필터·테마 중심 오차 0px, 필터 터치 44×44px. 검색창 폭은 각각 184·254·624·624px다.
- 필터 50회 왕복 렌더 p95 9.7ms, 최대 17.4ms, 중복 곡 ID 0이다.
- 작은 글자 최저 대비는 목록 key/time 다크 모드의 4.95:1이다. 현재 조·재생 선택 대비는 라이트 16.70:1, 다크 14.78:1이다.
- 측정용 headless Chrome과 ASCII 임시 복사/프로필/스크린샷은 종료·삭제했다. 저장소 작업 트리는 기능 커밋 직후 깨끗했다.

### 다음 우선 작업

1. 사용자가 앞서 요청한 “악보 Markdown 수정 → 웹 자동 반영”은 아직 구현하지 않았다. 현재 `tools/build.py`는 `src/data.js`를 인라인할 뿐 `tools/parse.py`를 실행하지 않는다. 아래 §6의 단일 빌드 진입점이 다음 핵심 작업이다.
2. 파서 게이트 실패 063·206·208·211은 아래 §4 원인부터 확인해야 한다. 실패 곡을 조용히 누락시키지 않는다.
3. 실제 iPhone/iPad Safari에서 장시간 자동 스크롤, 화면 잠금 방지, 노치 안전영역은 아직 실기기 체감 확인이 필요하다.
4. 즐겨찾기 전용 필터는 구현 완료했다. 계정 기반 클라우드 동기화는 요청하지 않았으므로 구현하지 않았고, 즐겨찾기는 현재 브라우저의 localStorage에만 저장된다.

### 이 문서 갱신 규칙

- 사용자 요청을 하나 완료할 때마다 이 “세션 재개용 현재 상태”의 시각·최신 커밋·완료 항목·검증값·남은 작업을 먼저 갱신한다.
- 기능 구현과 문서 내용이 다르면 소스와 실제 측정을 기준으로 문서를 즉시 바로잡는다.
- 새 세션은 먼저 `git status --short`, `git log -1 --oneline`, `python web/tools/build.py`, `node web/tools/test_engine.js`로 이 스냅샷이 아직 유효한지 확인한다.

### Codex → Claude Code 인계 원칙

- Codex 소유 범위에서 안전하게 끝낼 수 있는 UI·CSS·앱·빌드·배포 작업은 Codex가 직접 구현하고 검증한다.
- 파서·엔진·악보 원본처럼 Claude Code 소유 파일을 고쳐야 하거나, 데이터 계약·게이트 실패처럼 섣부른 우회가 위험한 작업은 억지로 수정하지 말고 `Claude Code 인계`로 명시한다.
- 어렵다는 이유만으로 바로 넘기지 않는다. 먼저 읽기 전용 진단과 재현으로 원인을 좁히고, 소유 범위 안의 안전한 대안까지 확인한다.
- 인계할 때는 반드시 증상, 재현 명령, 기대값/실제값, 관련 파일·행, 이미 시도한 내용과 결과, 수정 금지 파일, 완료 판정을 이 문서에 남긴다.
- 부분 구현이나 깨진 생성물을 남긴 채 넘기지 않는다. 마지막 정상 커밋과 작업 트리 상태를 적고, 미완성 변경은 Claude Code가 구분할 수 있게 정확히 표시한다.

## 0. 사용자 결정과 작업 경계

- 공개 GitHub Pages 사이트로 배포한다.
- 외부 런타임 의존성, 웹폰트, CDN을 추가하지 않는다.
- 최종 배포물은 단일 `index.html`이며 GitHub Pages는 저장소 `main` 브랜치 루트에서 제공한다.
- Codex 소유: `src/song of songs.html`, `src/app.css`, `src/app.js`, `tools/build.py`, `README.md`, Git 저장소와 배포 설정.
- Claude Code 소유: `tools/parse.py`, `src/engine.js`, `tools/test_engine.js`, `../악보/*.md`.
- Claude Code 소유 파일은 Codex가 수정하지 않는다. 반대로 자동화 구현 시에도 `parse.py` 자체를 고치지 말고 `build.py`에서 호출하는 방향을 우선한다.
- 기존 루트 `../CODEX.md`는 악보 전사 협업 기록이다. 이 웹 인계 문서와 용도가 다르므로 덮어쓰거나 합치지 않는다.

## 1. 결론

악보 Markdown 수정이 웹에 자동 연동되지 않는 원인은 빌드가 두 단계로 분리되어 있기 때문이다.

```text
../악보/*.md + ../곡목록.md
        │
        │ python tools/parse.py
        ▼
src/data.js
        │
        │ python tools/build.py
        ▼
index.html
```

현재 `tools/build.py`는 `tools/parse.py`를 실행하지 않는다. 이미 존재하는 `src/data.js`를 읽어 `index.html`에 인라인할 뿐이다. 따라서 `build.py`만 반복 실행하면 HTML 생성 시각은 최신이지만 악보 내용은 과거 `data.js` 스냅샷으로 남는다.

## 2. 2026-09-04 실측 증거

진단 당시 파일 시각:

| 파일 | 마지막 수정 시각 |
|---|---|
| `src/data.js` | 14:06:33 |
| 최신 악보 `../악보/207_교회의 기능.md` | 15:24:35 |
| `../곡목록.md` | 15:25:43 |
| `index.html` | 15:39:51 |

- `data.js` 이후 수정된 악보는 19개였다.
- 그중 파싱 결과가 실제로 달라진 악보는 7개다.
- 나머지 12개는 저장 시각만 새롭고 파서가 만드는 구조화 데이터는 기존 값과 같았다.
- 당시 `data.js`의 곡 수는 181곡이다.
- 최신 원본을 읽는 비파괴 모의 파싱 결과, 정상 포함 가능한 곡은 184곡이다.
- `index.html` 안에는 당시 `data.js` 문자열이 그대로 들어 있음을 확인했다.

## 3. 진단 당시 미반영 범위와 현재 조치

아래 7곡은 진단 당시 미반영 상태였다. 2026-09-04 같은 세션에서 `parse.py → test_engine.js → build.py`를 수동 실행해 현재 `data.js`와 `index.html`에는 모두 반영했다. 자동 연동 자체는 아직 구현하지 않았다.

### 기존 곡의 변경 내용 미반영

| 곡 | 달라진 파싱 영역 |
|---:|---|
| 001 | 2절, 3절, 4절 |
| 012 | 2절 |
| 049 | 마디 수, 1절, 1절 영문 |
| 064 | 1절 |

### 곡 전체 미포함

- 207 `교회의 기능`
- 217 `생명의 길`
- 226 `주와 합하는 자는 한 영(New Tune)`

이 세 곡은 최신 원본 기준 `parse.py`를 정상 통과했다. 재파싱 후 데이터 곡 수는 181곡에서 184곡으로 늘었다.

## 4. 최신 원본에도 남아 있는 파서 게이트 실패

다음 네 곡은 자동화를 구현해도 현재 원본 형식 그대로는 계속 제외된다.

| 곡 | 현재 오류 |
|---:|---|
| 063 | `(D)`가 허용된 코드 문법과 맞지 않음 (`../악보/063...md` 72행 부근) |
| 206 | 1절 2보표 마디선 열 불일치 및 코드/가사 2행 짝이 깨진 3행 구조 |
| 208 | `(Fm)`이 허용된 코드 문법과 맞지 않음 (`../악보/208...md` 37행 부근) |
| 211 | 마지막 보표가 3행 구조라 코드 줄 하나가 가사 없이 남음 (`../악보/211...md` 31~33행 부근) |

중요: 현재 `parse.py`는 일부 완료 곡이 게이트에 실패해도 그 곡만 제외하고 `data.js`를 쓰며 프로세스 종료 코드는 0일 수 있다. 단순히 subprocess 성공 여부만 검사하면 누락을 정상 빌드로 오판한다.

## 5. 파서가 입력으로 인정하는 범위와 함정

- `../악보` 바로 아래의 `*.md`만 읽는다. 하위 폴더는 읽지 않는다.
- 파일명의 첫 세 글자를 곡 ID로 사용한다.
- `../곡목록.md` 표에서 상태 문자열에 `완료`가 들어간 곡만 대상으로 삼는다.
- 섹션 제목은 정확히 `## `로 시작해야 한다.
- 코드 펜스 안에서 `|`가 있는 행을 코드 줄과 가사 줄 두 개씩 짝지어 읽는다.
- 마디선의 표시 열이 두 줄에서 다르거나 코드 정규식에 맞지 않으면 그 곡 전체가 제외된다.
- 중복 ID를 파서가 명시적으로 막지 않는다. 앱 라우팅은 같은 ID가 있으면 마지막 데이터를 선택할 수 있다.
- 상태 판정이 정확 일치가 아니라 부분 문자열 검사이므로 미래에 `미완료` 같은 값을 쓰면 잘못 포함될 여지가 있다.
- 진단 당시 실제 파일에는 비숫자 ID 0, 중복 ID 0, 곡목록 누락 0이며 188개 행 모두 `완료`였다.

## 6. 권장 자동화 — 1순위

`tools/build.py`를 단일 진입점으로 만든다.

```text
python tools/build.py
  1. 현재 Python으로 tools/parse.py 실행
  2. 파서 출력에서 게이트 실패 곡/개수 확인
  3. node tools/test_engine.js 실행
  4. CSS·JS·data.js를 index.html에 인라인
  5. 결과 곡 수, 제외 ID, 외부 참조 수를 한 번에 출력
```

구현 원칙:

- 표준 라이브러리 `subprocess`만 사용한다.
- 어느 폴더에서 실행해도 되도록 절대 경로는 `Path(__file__)`에서 계산한다.
- `parse.py`는 수정하지 않는다.
- 파서가 종료 코드 0을 반환해도 출력의 `게이트 실패`를 별도로 감지한다.
- 게이트 실패가 있을 때 완전 중단할지, 명확한 경고 후 유효 곡만 만들지는 사용자에게 확인한다.
- `data.js`와 `index.html`은 생성물이므로 직접 편집하지 않는다.
- 파싱 또는 엔진 테스트 실패 시 이전 `index.html`을 덮어쓰지 않는 것이 안전하다.

현재 사용자가 원하는 핵심은 적어도 “악보 수정 후 `build.py` 한 번만 실행하면 최신 데이터까지 반영”되는 것이다.

## 7. 선택 자동화 — `--watch`

저장 즉시 반영이 필요하면 `build.py --watch` 또는 별도 표준 라이브러리 watcher를 둔다.

감시 대상:

- `../악보/*.md`
- `../곡목록.md`
- `src/song of songs.html`
- `src/app.css`
- `src/app.js`
- `src/engine.js`

권장 동작:

- 각 파일의 `(mtime_ns, size)` 스냅샷을 약 0.5초 간격으로 비교한다.
- 편집기의 임시 저장/rename 연속 이벤트를 흡수하도록 약 0.3초 debounce한다.
- 악보나 곡목록 변경: `parse → test → build`.
- UI 소스 변경: `build`만 실행해도 된다.
- 생성물 `src/data.js`, `index.html`, `__pycache__`는 감시하지 않는다. 포함하면 무한 재빌드될 수 있다.
- `watchdog`, Live Server 같은 외부 패키지는 추가하지 않는다.

## 8. 브라우저 자동 새로고침은 별도 문제

열려 있는 `file://.../index.html`은 디스크의 HTML이 바뀌어도 메모리의 페이지를 자동 교체하지 않는다. watcher가 빌드만 하면 사용자가 새로고침해야 한다.

완전 자동 새로고침이 필요할 때의 권장 개발 모드:

- Python 표준 라이브러리 `ThreadingHTTPServer`로 `127.0.0.1`에서만 제공한다.
- 개발 서버 응답에만 빌드 버전 폴링을 주입하고, 배포용 `index.html`은 오염시키지 않는다.
- 빌드 버전이 바뀌면 페이지를 reload한다.
- reload 시 현재 스크롤 위치와 현재 곡/이조 상태 일부가 초기화될 수 있음을 알린다. 저장된 테마·글자 크기·속도는 `localStorage`로 유지된다.

## 9. GitHub Pages 구조 주의

Git 저장소 루트는 현재 지시대로 `web/`로 잡는다. 이 방식은 PDF, 원본 악보, 작업용 도구 전체가 공개 저장소에 섞이지 않고 웹에 필요한 파일만 게시하기에 적합하다.

현재 배포 설정:

- 공개 저장소: `https://github.com/songil23/song-of-songs`
- 공개 사이트: `https://songil23.github.io/song-of-songs/`
- 기본 브랜치: `main`
- Pages 소스: `main` 브랜치의 저장소 루트(`/`)
- Pages 공개 상태: public
- HTTPS 강제: enabled

다만 `../악보`와 `../곡목록.md`는 이 저장소 바깥이다. 따라서 GitHub Actions가 원격 환경에서 Markdown을 다시 파싱하는 완전 자동 배포는 이 구조만으로는 불가능하다.

- 현재 구조: 로컬에서 `parse → test → build`, 생성된 `data.js`와 `index.html`을 push.
- 미래에 원격 자동 빌드가 필요할 때: 프로젝트 루트를 저장소로 바꾸되 공개 가능 파일을 엄격히 선별하거나, 악보 입력 전용 비공개 저장소/워크플로를 별도로 설계한다.

## 10. 검증 체크리스트

자동화 구현 후 최소 검증:

```powershell
python web/tools/build.py
node web/tools/test_engine.js
```

- 최신 유효 데이터 184곡이 포함되는지 확인한다.
- 207·217·226이 목록과 직접 URL 해시에서 열리는지 확인한다.
- 001·012·049·064의 최신 절/마디 데이터가 반영됐는지 원본과 대조한다.
- 063·206·208·211의 제외 이유가 빌드 로그에 눈에 띄게 나오는지 확인한다.
- `index.html`에 외부 HTTP(S) 참조가 0인지 확인한다.
- 390px에서 페이지 가로 넘침 0, 왼쪽 코드 잘림 0인지 확인한다.
- 브라우저 콘솔 오류가 0인지 확인한다.

## 11. 현재 웹 UI 상태

- 즐겨찾기는 목록과 곡 제목의 별 버튼으로 전환하며 `song-of-songs.favorites.v1`에 곡 ID 배열로 저장한다. 상단 별 필터는 검색과 AND로 결합해 즐겨찾기 행만 표시한다.
- 자동 스크롤은 속도 1~15(1.5~22.5px/s)이고, 정수 스크롤 사이를 1px 미만 transform으로 보간한다.
- 상단 테마·즐겨찾기 SVG는 44px 버튼의 정중앙에 맞추며 재생/정지 SVG는 `hidden` 속성을 직접 전환한다.
- 왼쪽으로 시각적으로 돌출되는 코드에 동적 보호 여백을 적용했다.
- 긴 접미 코드와 slash bass가 다음 코드와 겹치지 않도록 열 단위 여백을 동적으로 확보한다.
- 코드와 가사 셀에는 같은 보정량을 적용해 중심 정렬을 유지한다.
- 12px·16px·24px 설정의 기본/쌓기/대표 이조 총 1,221상태에서 왼쪽 잘림 0, 코드 충돌 0, 페이지 가로 넘침 0을 확인했다.
- 마디 여백 토큰은 `.35em`에서 `.4em`으로 소폭 늘렸다.
- 최신 데이터 재생성 후 엔진 회귀 검사 결과는 184곡, 코드 116종, 쌓기 가능 96곡, 실패 0이다.

## 12. 후속 작업 완료 기준

Claude Code가 자동 연동을 이어서 구현한다면 다음을 모두 만족해야 완료다.

1. `build.py` 한 번으로 최신 악보 파싱부터 단일 HTML 생성까지 끝난다.
2. 게이트 실패 곡이 조용히 누락되지 않는다.
3. 실패한 중간 단계가 기존 정상 `index.html`을 손상시키지 않는다.
4. 외부 의존성 0을 유지한다.
5. 보호 파일을 수정했다면 반드시 소유자/사용자 승인과 변경 이유가 기록된다.
6. watcher를 넣는 경우 생성물 감시로 인한 재귀 빌드가 없다.
7. GitHub Pages 배포본을 실제 URL에서 열어 목록·곡·이조·쌓기·확대·자동 스크롤을 확인한다.
